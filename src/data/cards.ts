export interface CardData {
  id: number;
  title: string;
  part: "sdlc" | "api";
  section: string;
  frontContent: string[];
  backQuestions: string[];
}

export const cards: CardData[] = [
  {
    id: 1,
    title: "Work Items & Traceability",
    part: "sdlc",
    section: "1.1",
    frontContent: [
      "The unit of work is the ticket. Every piece of code change traces back to a ticket; every release reports which tickets it contains. This chain is the load-bearing structure of the entire process — without it, the team cannot answer basic questions about what is in production, why it exists, or when it changed.",
      "Tickets are typed. The minimum useful taxonomy:",
      "Story — User-facing capability. Has explicit acceptance criteria written before work begins.",
      "Bug — Defect against existing behavior. Has reproduction steps and a severity rating.",
      "Task — Technical work without direct user value — refactoring, infrastructure, technical debt. Has a stated rationale.",
      "Spike — Time-boxed investigation. The output is a written decision or recommendation, not shipped code.",
      "Each ticket has a unique identifier — for example, PROJ-1234. That identifier flows through every downstream artifact:",
      "Branches are named with the ticket ID: feat/PROJ-1234-rabbit-backpressure",
      "Pull requests reference the ticket in their description: Closes #1234",
      "Merge commits carry the ticket reference into the git history",
      "Release notes list which tickets were resolved in each release",
      "Tickets close automatically when the release containing them ships to production",
      "Tickets are organized into hierarchy. Epics or milestones group related work; sprints time-box delivery. The hierarchy serves planning and reporting, not execution — the unit of work remains the individual ticket.",
      "Base level recommendation: The four ticket types above, with ticket IDs flowing through branch names, PR descriptions, commits, and release notes. Acceptance criteria mandatory on stories before they are accepted into a sprint. Story-point estimation and velocity tracking are deferred until the team has the size and stability to make those numbers meaningful.",
    ],
    backQuestions: [
      "How will we enforce ticket IDs in branch names and pull requests?",
      "Who is responsible for writing the acceptance criteria before work begins?",
      "With no product manager in the org, who acts as the interim backlog owner, and what work do we pilot the ticket taxonomy on before a product exists?",
    ],
  },
  {
    id: 2,
    title: "Branching Model",
    part: "sdlc",
    section: "1.2",
    frontContent: [
      "The branching model is determined by the deployment cadence target. Continuous delivery — where any commit to the main branch is a candidate for production — rules out heavyweight models like GitFlow that maintain parallel long-lived branches. The model that fits is trunk-based development, in either its strict form (multiple commits per day directly to main, with feature flags) or its lightweight form (GitHub Flow, with short-lived feature branches that merge to main via pull requests).",
      "The fundamental decision: one source-of-truth branch (main), with environments as deployment targets, not branches.",
      "This decoupling is worth being explicit about, because it contradicts a common intuition. The intuitive model is that there should be a \"dev branch\" that deploys to the development environment, a \"staging branch\" for staging, and a \"main branch\" for production. This model is structurally incompatible with continuous delivery, and it ages badly: the branches drift, the diffs between them grow terrifying, and releases require risky merges of accumulated changes.",
      "The correct model: feature branches merge to main via pull request. Each merge to main produces a single built artifact (a container image, identified by both a semantic version tag and the git commit SHA). That same artifact is promoted through environments — dev, staging, production — with environment-specific configuration injected at runtime. The image is identical in every environment; only configuration varies.",
      "This produces a number of properties that matter:",
      "There is one definitive answer to \"what is in production right now\": the image tagged with the latest release version on main.",
      "There is one definitive answer to \"what did QA test\": the same image bits that will deploy to production.",
      "Rollback is trivial: redeploy the previous image tag. No git operations required.",
      "Hotfixes follow the normal flow when possible (a fast fix on main, fast release); they require special handling only when main has unreleased work that cannot ship.",
      "Base level recommendation: GitHub Flow with a discipline of short-lived feature branches (target: under three days). Branch naming follows the pattern type/TICKET-ID-short-slug. Direct pushes to main are prohibited by branch protection.",
    ],
    backQuestions: [
      "What is our deployment cadence target?",
      "Can we commit to keeping feature branches alive for less than three days?",
      "Which repository or pilot project adopts trunk-based development first, and do we have enough developers to review PRs fast enough to keep branches short-lived?",
    ],
  },
  {
    id: 3,
    title: "Issue Lifecycle",
    part: "sdlc",
    section: "1.3",
    frontContent: [
      "An issue moves through a defined set of states from creation to closure. The states are not merely organizational — they correspond to operational states of the code change itself, and the transitions are events that can be automated.",
      "Two ideas in this lifecycle are load-bearing and worth surfacing:",
      "\"Done\" means \"in production,\" not \"merged.\" If \"done\" is defined as \"merged to main,\" then nobody owns the gap between merge and production. Tickets accumulate in a half-shipped state, and the project board diverges from reality. Tying \"done\" to a tagged production release closes that gap and makes the release tag — see section 1.5 — the artifact that actually closes work.",
      "\"Ready\" is a gate, not just a state. A ticket is not pullable simply because it exists. Acceptance criteria, scope, and dependencies must be defined before development begins. Without this gate, developers scope tickets at code-writing time, which is when requirements drift starts. In the absence of a dedicated product manager, the gate is held by Engineering leadership at sprint planning.",
      "Backward transitions matter. \"In Review\" can return to \"In Progress\" when review surfaces required changes. \"In QA\" can return to \"In Progress\" when QA finds a defect. The board reflects this honestly rather than pretending work flows linearly.",
      "Base level recommendation: Adopt the full six-state lifecycle. The \"Ready\" and \"In QA\" gates are aspirational pending dedicated product management and QA roles; in the interim, Engineering leadership approves \"Ready\" during sprint planning, and \"In QA\" is satisfied by automated tests and smoke tests against the staging environment. The lifecycle is encoded in GitHub Projects automation so cards move automatically based on PR and release state, rather than relying on developers to manually update them.",
    ],
    backQuestions: [
      "Who holds the \"Ready\" gate to prevent requirements drift?",
      "With no dedicated product management or QA roles, which named person approves \"Ready\" and what satisfies \"In QA\" in the interim?",
      "What conditions would trigger us to create the dedicated product manager and QA roles this lifecycle assumes?",
    ],
  },
  {
    id: 4,
    title: "Pull Request Mechanics",
    part: "sdlc",
    section: "1.4",
    frontContent: [
      "The pull request is the unit of code review and the unit of merge. Its quality is determined by two things: scope discipline and review norms. The mechanics that follow are best understood as encoding agreements that the team has to make consciously rather than allowing to evolve by accident.",
      "Scope discipline",
      "One PR corresponds to one ticket. This is a strong default rather than an absolute rule, but the rationale is structural: multi-ticket PRs are reviewable in theory and unreviewable in practice. Review comments scatter across unrelated changes; \"while I'm here\" creep makes the diff incoherent; the reviewer's attention thins across topics. If a single ticket requires multiple PRs, that is a signal the ticket was scoped too large and should have been split.",
      "PR size targets follow from research on review effectiveness: review quality drops sharply above approximately 400 lines of changed code. The team's default target is under 200 lines where possible. This is not a hard wall — data model migrations, framework upgrades, and similar changes legitimately resist further decomposition — but it is the default.",
      "Three mechanisms support keeping PRs small in practice:",
      "Tickets sized to fit in a single PR (the Ready gate enforces this implicitly).",
      "Refactor changes separated from behavior changes — \"first PR moves things around without changing behavior; second PR adds the feature.\"",
      "Feature flags to land partial implementations safely, allowing code to ship dark and be activated later.",
      "Review norms",
      "Review norms are where the process becomes either useful or ceremonial. The mechanics are simple; the cultural commitment is the hard part.",
      "Required approvals. At least one reviewer who is not the author. Critical paths — authentication, payments, data model changes, infrastructure — require two. CODEOWNERS rules in the repository automatically assign reviewers based on which files changed.",
      "What review actually checks. Worth writing down explicitly so reviewers know what they are doing: Does the code do what the ticket says? Is it correct under edge cases? Is it safe (no secrets, no injection vectors, no auth bypasses)? Is it observable (appropriate logging, metrics where they matter)? Is it tested at the level the change warrants? Will the next person understand it in six months? Does it fit the existing system, or intentionally diverge with a stated reason?",
      "What review does not check. Style, formatting, naming bikesheds. These are automated via linters and formatters. If a reviewer is leaving comments about whitespace, the tooling is misconfigured, not the author.",
      "Comment resolution. Distinguish blocking comments (\"this is wrong\" or \"this is unsafe\" — must be addressed before merge) from non-blocking comments (\"nit:\" or \"suggestion:\" — author may address or defer with explanation) from questions (author answers; may or may not result in code change). GitHub does not enforce this distinction, but a team convention of prefix labels makes intent explicit and reduces the friction of \"do I have to address everything.\"",
      "Author and reviewer responsibilities",
      "Often left implicit, both should be written down:",
      "Author: Links the ticket in the PR description. Explains why, not what — the diff shows what. Self-reviews their own diff before requesting review, catching the obvious issues. Responds to every comment, even if only to acknowledge. Does not force-push during review, because it breaks the reviewer's ability to see incremental changes.",
      "Reviewer: Reviews within one to three business days of being requested — the cost of stale PRs compounds. Approves, requests changes, or comments — does not leave PRs in limbo. Reviews the whole change, not just the first file. Pulls the branch and runs it for anything non-trivial.",
      "Merge mechanics",
      "Squash-merge by default: one commit per PR on main, clean history, the PR title becomes the squash commit message.",
      "PR title follows Conventional Commits format (see section 1.5), validated by CI.",
      "Branches are deleted after merge.",
      "The author merges their own PR after approval, not the reviewer — the author owns the timing of the merge.",
      "Base level recommendation: PR template requiring ticket link. One required reviewer. CODEOWNERS for any critical paths identified. Squash-merge with Conventional Commits PR titles. Branch protection on main: required status checks, required reviews, no force push, no direct push.",
    ],
    backQuestions: [
      "What are our critical paths that will always require two reviewers?",
      "Given current team size, can we guarantee one non-author reviewer per PR within the review SLA — and what happens when we can't?",
      "Who maintains CODEOWNERS when code ownership areas don't exist yet?",
    ],
  },
  {
    id: 5,
    title: "Releases & Versioning",
    part: "sdlc",
    section: "1.5",
    frontContent: [
      "A release is not \"whatever happens to be on main this week.\" It is an explicit, versioned bundle of tickets, tagged at a specific commit, reproducible, and traceable. The release tag is the artifact that ties together everything the SDLC produces: the resolved tickets, the built image, the deployed environments, and the consumer-facing documentation.",
      "Semantic versioning, driven by commit conventions",
      "Releases follow semantic versioning: MAJOR.MINOR.PATCH. The version is computed automatically from the commit history since the previous release tag, using Conventional Commits to determine the bump:",
      "fix: commits bump the patch version.",
      "feat: commits bump the minor version.",
      "feat! or BREAKING CHANGE: footer bumps the major version.",
      "chore:, refactor:, docs:, test: commits do not trigger a version bump on their own.",
      "Conventional Commits is load-bearing in this proposal. If commits are not formatted correctly, version computation breaks and release notes degrade to noise. The format is enforced by a commitlint check on PR titles in CI; PRs with malformed titles cannot merge.",
      "Release cadence",
      "Three cadence models are possible:",
      "Continuous — Every merge to main produces a release and ships to production. Cadence equals merge cadence. Requires mature observability and rollback automation.",
      "Time-boxed — Releases happen on a fixed schedule — typically end of sprint. Whatever has merged since the last release is included.",
      "Scope-boxed — Releases happen when a defined set of work is complete. Calendar-flexible, scope-fixed.",
      "The target for this proposal is continuous release: every merge flows automatically through environments. The initial state is time-boxed, end-of-sprint releases, with the pipeline running continuously but the final promotion to production gated by manual approval. This gives the team CD-shaped mechanics with a release rhythm that humans can reason about while observability matures.",
      "The migration from time-boxed to continuous is a policy change, not an engineering project, once three prerequisites are in place: automated detection of bad deploys, automated rollback when detection fires, and feature flag discipline that keeps main releasable at all times.",
      "Release mechanics",
      "Releases are created by automation. The team does not hand-craft tags, version numbers, or release notes. The flow:",
      "A workflow is triggered, either automatically on merge to main (continuous) or by manual dispatch (time-boxed).",
      "Release tooling (release-please or semantic-release) reads commits since the previous tag, computes the next version, and creates a release branch with the new version number and an auto-generated changelog.",
      "Merging the release branch creates an annotated git tag — for example, v2.7.4.",
      "CI builds the container image, tags it with both the semantic version and the git commit SHA, and pushes it to the container registry.",
      "The image is promoted through dev, staging, and production environments.",
      "Tickets referenced via \"Closes #123\" in PR descriptions are auto-closed when their release ships to production.",
      "A release notes summary is posted to a notification channel.",
      "Release tags as the unit of QA truth",
      "The release tag is what makes QA work meaningful. QA cannot test \"the current state of main\" — that state moves under them. They can test \"release v2.7.4,\" because the tag is immutable and is bound to a known set of tickets via the release notes. A bug found in v2.7.4 stays found; a regression caught in v2.7.5 is unambiguous. The tag is the artifact under test.",
      "Base level recommendation: Time-boxed releases at the end of each two-week sprint, created via manual workflow dispatch. release-please for automation (preferred over semantic-release for its GitHub-native PR-based flow). Conventional Commits enforced via commitlint on PR titles. Images tagged with both semver and SHA. Release notes auto-generated from commits, with a brief human-written highlights summary added to each release. Migration to continuous release is a stated target, contingent on observability investment.",
    ],
    backQuestions: [
      "Are we starting with time-boxed sprint releases or moving directly to continuous releases?",
      "What is the pilot product or service we version and release first, given none exists today?",
      "Who acts as release manager and approves production promotion while those roles are unfilled?",
    ],
  },
  {
    id: 6,
    title: "Environments & Configuration",
    part: "sdlc",
    section: "1.6",
    frontContent: [
      "Three environments are the minimum that makes the pipeline meaningful: development, staging, and production. Each runs the same container image, with environment-specific configuration injected at runtime.",
      "The deployment flow:",
      "feature branch --PR--> main --build--> image:v2.7.4 (also tagged image:abc123)\n  +-- automatic ---> dev environment\n  +-- automatic ---> staging environment\n  +-- manual approval ---> production environment",
      "What varies between environments is configuration: database connection strings, feature flag values, scaling parameters, external service endpoints. These are externalized — they live in Azure App Configuration and Azure Key Vault, not in the image. The image cannot know which environment it is running in, and that is the correct property: it forces a clean separation of code from configuration.",
      "This produces the property that what QA tested in staging is what gets promoted to production. Not \"the same code, built differently.\" The same bits.",
      "Configuration in three layers",
      "Configuration is one word covering three distinct problems:",
      "Static configuration: Values that vary by environment but do not change at runtime. Database hostnames, log levels, replica counts. Read once at startup. Stored in Azure App Configuration, injected as environment variables.",
      "Secrets: Sensitive values — credentials, API keys, certificates. Same delivery pattern as static config but with different storage: Azure Key Vault, accessed via Managed Identity, audited and rotatable. Never committed to source control, never logged.",
      "Feature flags: Values that change at runtime without redeploying. Toggles for features, percentage rollouts, per-tenant configuration. Read continuously by the application; the SDK polls and caches briefly. Azure App Configuration supports feature flags as a first-class concept for greenfield use.",
      "These three problems require three different tools because they have different access patterns and different lifecycles. Conflating them is the source of a substantial fraction of production incidents. Static configuration in a feature flag service is wasteful. Secrets in environment variables without rotation discipline are leaky. Feature flags in static configuration require a deploy to toggle, defeating their entire purpose.",
      "Feature flag discipline",
      "Feature flags are the foundation that makes trunk-based development viable. They decouple deployment from release: code can ship to production behind a flag in the off position, then be activated later. This lets the team merge incomplete work to main without violating the \"main is always releasable\" rule.",
      "Flags accumulate debt. Each one is a code path that exists, must be maintained, and adds cognitive load. To prevent debt accumulation, flags are categorized and managed differently by category:",
      "Release — Decouples deploy from release. Short-lived: days to weeks. Has an owner and a removal date at creation.",
      "Experiment — A/B testing, percentage rollouts to measure impact. Medium-lived: weeks to months.",
      "Operational — Kill switches, circuit breakers, debug toggles. Long-lived: indefinite. Permanent operational levers.",
      "Permission — Per-tenant or per-user gating, often tied to subscription tier. Permanent, but managed as business policy rather than engineering state.",
      "CI lints for orphaned flag references — code that checks a flag no longer defined in the configuration service. Quarterly flag audits remove dead flags. Without these mechanisms, flag systems accumulate to hundreds of stale toggles within a year.",
      "Base level recommendation: Three environments (dev, staging, prod). Same container image in each. Static config in Azure App Configuration, injected as environment variables. Secrets in Azure Key Vault, accessed via Managed Identity, injected as environment variables for most cases and mounted as files via CSI driver for certificates. Feature flags in Azure App Configuration (native feature flag support is sufficient for pilot scale; LaunchDarkly or similar is a future migration when targeting requirements outgrow it). Flag taxonomy enforced via naming conventions and PR review.",
    ],
    backQuestions: [
      "How will we categorize and audit feature flags to prevent technical debt?",
      "Who provisions and owns the three Azure environments — and who approves that budget — when no platform team or product exists yet?",
    ],
  },
  {
    id: 7,
    title: "CI/CD Pipeline",
    part: "sdlc",
    section: "1.7",
    frontContent: [
      "The pipeline is the load-bearing artifact of this proposal. Every concept described elsewhere — issue lifecycle, branching, PR mechanics, release management, environment promotion — depends on the pipeline enforcing it. A process described in a wiki but not encoded in pipeline configuration degrades to vibes within a quarter. The YAML is the contract.",
      "The pipeline is organized in three phases corresponding to three trigger contexts:",
      "PR pipeline: runs on every push to a feature branch with an open PR. Goal: validate the change in isolation before merge.",
      "Main pipeline: runs on merge to main. Goal: produce the deployable artifact, deploy to dev and staging, validate.",
      "Release pipeline: runs on release tag creation. Goal: promote the validated artifact to production.",
      "Each phase is composed of stages. Each stage is a gate that can fail and stop progression. Stages are ordered by cost and scope: cheap, fast, isolated checks run first; expensive, slow, deployment-dependent checks run later. The pipeline finds problems as early as possible, both because feedback is faster and because broken builds do not consume downstream resources.",
      "Phase 1: PR pipeline",
      "Triggered on every push to a feature branch with an open PR. All stages are required status checks; the PR cannot merge unless every stage passes.",
      "Stages run in parallel where dependencies permit. Total wall-clock time target: under eight minutes for the full PR pipeline. Beyond ten minutes, the pipeline becomes a context-switch cost rather than a feedback loop.",
      "Phase 2: Main pipeline",
      "Triggered on push to main, immediately after PR merge.",
      "Re-run all PR pipeline stages on main's actual content (defensive — main may have moved between when CI last ran on the PR and when it merged).",
      "Build container image, tag with semver and git SHA, push to Azure Container Registry.",
      "Scan image with Trivy or Microsoft Defender for Containers. Block on critical CVEs.",
      "Deploy image to development environment automatically.",
      "Run smoke tests against development: health endpoint, critical-path API calls, infrastructure connectivity checks. Five to ten checks max — \"did the deploy work,\" not \"is the system correct.\"",
      "Deploy image to staging environment automatically on dev smoke success.",
      "Run end-to-end tests against staging: Playwright or Cypress with a small release-blocking suite covering critical user journeys, plus a larger advisory suite of exploratory coverage.",
      "At the end of Phase 2, the image has been built, scanned, pushed, deployed to development and staging, and validated in both. It is a release candidate.",
      "Phase 3: Release pipeline",
      "Triggered when a release tag is created.",
      "Validate that the tag points at a commit which passed the full Phase 2 pipeline.",
      "Validate that the corresponding container image exists in the registry and is signed.",
      "Manual approval gate: a named reviewer approves the production deployment in the GitHub UI.",
      "Deploy the tagged image to production. Container Apps revision swap with progressive rollout (configurable percentage to new revision before full promotion).",
      "Run post-deploy smoke tests against production. On failure, automatic rollback to previous revision.",
      "Tickets referenced by the release auto-close. Project board entries transition to Done.",
      "Publish the OpenAPI specification for the release to the hosted documentation site (see Part 2).",
      "Post release notes to a notification channel.",
      "What blocks vs. what is advisory",
      "The taxonomy of pipeline outcomes is itself a governance decision:",
      "Required (blocks merge or promotion): All build, lint, format jobs. Unit and integration tests. Critical security findings. Breaking change detection on API specs. Commit message format. Smoke tests at each environment. End-to-end release-blocking suite.",
      "Advisory (reported, does not block): Code complexity and duplication findings. Low-severity CVEs in transitive dependencies. Non-critical static analysis findings. End-to-end exploratory suite.",
      "Informational (logged for visibility): Coverage metrics until ratcheted to required. Build time trends. Test flake rates.",
      "The advisory category is load-bearing. It lets the team see signals without being held hostage by them. The common failure mode is putting everything in \"required,\" discovering that thirty percent of the time the pipeline is red for reasons that do not actually matter, and then training the team to ignore failures — which defeats the entire purpose.",
      "Base level recommendation: All three phases implemented. Phase 1 includes lint, build, unit tests, dependency scan, integration tests, commitlint as required gates; static analysis and OpenAPI diff added once those tools are configured (week two or three of pilot). Phase 2 includes re-run of PR stages, image build, image scan, deploy to dev with smoke tests. Phase 3 includes manual approval, deploy to prod, post-deploy smoke tests, ticket closure, documentation publication. End-to-end automated tests in staging are deferred until a worthwhile suite exists; in the interim, staging deploys are validated manually.",
    ],
    backQuestions: [
      "Which checks must actively block a merge versus acting as advisory warnings?",
      "Who builds and maintains the pipeline itself when no DevOps or platform engineering role exists?",
      "Without a product to ship, do we stand up the pipeline against a pilot repository first to prove the mechanics end-to-end?",
    ],
  },
  {
    id: 8,
    title: "Hotfix Flow",
    part: "sdlc",
    section: "1.8",
    frontContent: [
      "A hotfix is not a category of code; it is a category of urgency. The code change itself may be a one-line null check. What makes it a hotfix is operational context: production is degraded or broken, and the standard release cadence is too slow to address it.",
      "This distinction matters because the temptation is to invent a separate process for hotfixes — different branches, different review rules, different deploy paths. That is almost always the wrong response. The right response is the same process executed faster.",
      "Two scenarios, two responses",
      "Scenario 1: the bug is on main but not yet released to production. This is not actually a hotfix. Production is unaffected. The team holds the release until the fix lands, then ships normally. The urgency is procedural, not operational.",
      "Scenario 2: the bug is in production. Production is running an old release tag; main has moved on with unreleased work that cannot ship as-is. This is the actual hotfix problem, and it has two possible responses.",
      "Response A — forward-fix from main. Fix the bug on main via the normal flow. When main is in a releasable state (in-flight features behind flags or otherwise contained), cut a new release and promote it to production. This works only if main is consistently releasable, which requires feature flag discipline.",
      "Response B — hotfix branch from the release tag. Branch from the production release tag, apply the surgical fix, build and deploy that branch as a new patch release. The fix is also merged into main (via PR to main first, then cherry-picked to the hotfix branch) so it is not lost in the next normal release.",
      "Policy: Default to Response A (forward-fix). Fall back to Response B when main is not in a releasable state. Track the ratio of B to A — frequent use of B is a signal that main hygiene has degraded and the team needs to invest in feature flags or smaller PRs. Hotfix branches are a failure mode indicator, not a feature.",
      "The emergency bypass",
      "Rarely, the standard pipeline is too slow even for hotfix flow. Data corruption in progress, active security incident, similar situations where the cost of staging promotion exceeds its benefit. The process must include a legitimate exception path.",
      "The mechanic: a separate workflow with manual dispatch only, requiring named approval (Director or above), deploying a specified image SHA directly to production. The image must still have been built from a real commit (the build pipeline is not bypassed). What is bypassed is environment promotion, not CI.",
      "Every invocation is logged in an incident channel and post-mortemed within a week. The post-mortem is not punitive — its purpose is to learn whether the bypass was necessary and whether the standard process needs to be faster. The bypass must be a blameless, legitimate option, because the alternative when it does not exist is not \"the standard process,\" it is \"someone edits files on a production pod under stress.\"",
      "Base level recommendation: Default to forward-fix; document both responses and their decision criteria. Emergency bypass implemented as a manually-dispatched workflow with named-approver gating. Hotfix frequency tracked as a process health metric. No skipping of CI under any path.",
    ],
    backQuestions: [
      "Under what conditions do we use the emergency bypass, and who is the named approver?",
    ],
  },
  {
    id: 9,
    title: "Versioning Strategy",
    part: "api",
    section: "2.1 & 2.2",
    frontContent: [
      "Two versions live in any API product, and conflating them produces durable confusion:",
      "Release version (for example, 2.7.4): Identifies a deployed artifact. Internal concern. Changes constantly. Tied to commits.",
      "API version (for example, v1): Identifies a consumer-facing contract. External concern. Changes rarely. Tied only to breaking changes.",
      "A single service running release 2.7.4 might simultaneously serve /api/v1/ and /api/v2/ endpoints. Different lifecycles, different audiences, different rules. Documentation needs to disambiguate both: the spec for /api/v1/ as it existed at release 2.7.4 is a different artifact from the spec for /api/v1/ as it exists today.",
      "Three strategies for exposing API versions are technically viable:",
      "URI path — /api/v1/orders — Obvious to consumers, easy routing, easy to log and cache. The conventional choice for enterprise APIs.",
      "Header — Accept: application/vnd.nfi.v1+json — URLs stay clean, but harder to test, less discoverable, more friction in client tooling.",
      "Query parameter — /api/orders?v=1 — Simple but pollutes the query namespace and is easy to forget.",
      "Recommendation: URI path versioning. The arguments against it tend to be aesthetic (\"REST purity\"); the operational arguments for it are concrete. Path versioning shows up in logs, in client code, in routing rules, in cache keys. Consumers expect it. Azure API Management routes it cleanly. It is the version strategy least likely to cause operational confusion.",
    ],
    backQuestions: [
      "Are we culturally committed to never breaking a published contract once it ships?",
      "With no API consumers today, at what moment does the contract freeze take effect — first published spec, first internal consumer, or first external partner?",
    ],
  },
  {
    id: 10,
    title: "Breaking Changes & Documentation",
    part: "api",
    section: "2.3, 2.4 & 2.5",
    frontContent: [
      "Breaking-change detection is the technical control that enforces the API versioning policy. The team needs an explicit, written contract about what counts as breaking, because the alternative is reviewer judgment, which decays over time.",
      "Breaking: Removing a field. Renaming a field. Changing a field's type. Tightening validation (a previously-accepted value is now rejected). Changing error codes. Changing authentication semantics. Adding a required field to a request body.",
      "Not breaking: Adding an optional field to a request. Adding a new field to a response (consumers should ignore unknown fields). Adding a new endpoint. Loosening validation (a previously-rejected value is now accepted). Adding new optional query parameters.",
      "The detection is automated. Stage 1.7 of the PR pipeline runs openapi-diff against the previous version's spec. If breaking changes are detected and the PR is not marked breaking (via Conventional Commits feat! or BREAKING CHANGE: footer), the build fails. This is the technical enforcement of the contract; without it, the rule depends on reviewer vigilance, which is unreliable under deadline pressure.",
      "Spec-first authoring is the corollary discipline. The OpenAPI specification is checked into the repository, reviewed in PRs, and is the source of truth. Code implements the spec; runtime validation enforces conformance. The alternative — code-first generation, where the spec is derived from code annotations at build time — makes breaking-change detection unreliable because generator quirks produce noise that swamps real signal.",
      "Every published API version has a lifecycle with defined phases. The phases are calendar events, not vibes — each transition is announced in advance and visible to consumers.",
      "GA (Generally Available) — Fully supported. All bug fixes and security patches backported. New backward-compatible features may be added (minor version bumps within the major).",
      "Maintenance — Security and critical bug fixes only. No new features backported. The successor version exists and is recommended.",
      "Deprecated — Announced end-of-life date. Consumers should migrate to the successor. Only critical security fixes applied. The Sunset HTTP header (RFC 8594) is added to responses.",
      "Retired — The endpoint returns 410 Gone with a Link header to the successor. The version is no longer served.",
      "Each phase has a default duration, set as policy:",
      "Major versions: three years of GA, followed by a defined migration period.",
      "Minor versions within a major: backward-compatible by definition, so consumers can upgrade transparently. \"Support for a minor\" specifically means backporting fixes to that minor; consumers on older minors are encouraged to upgrade to the latest minor of their major to receive patches.",
      "Patch versions: applied to the latest minor of each supported major. Consumers receive patches by staying on the latest minor.",
      "These durations are policy proposals, not facts. The cost of longer windows is parallel-version maintenance: every supported major requires the team to backport security fixes and critical bugs for the duration of its support. Longer windows are more consumer-friendly and more expensive. The proposed three-year major lifetime is generous but defensible for a partner-facing API; it should be revisited as the consumer base and team size evolve.",
      "For every release of every supported API version, the OpenAPI specification is published as a versioned, hosted, browsable artifact. Consumers can:",
      "Browse the current API contract interactively.",
      "Pin to a specific release's documentation.",
      "Diff between versions to see what changed.",
      "Download the raw spec to generate clients.",
      "Documentation structure reflects the three-axis identification problem: API contract version (in the path), release version (the snapshot), and \"latest\" pointers per API version.",
      "Hosting approach",
      "Two approaches serve different purposes and compose well:",
      "Azure API Management developer portal: Consumer-facing entry point. Discovery, subscription management, interactive \"try it out\" with native OAuth handling. This is what consumers bookmark.",
      "Static site (Azure Storage + Front Door): Version archive. Every historical release's spec, browsable, diffable. Cheap and operationally trivial. This is where consumers reference older versions.",
      "APIM does not handle version archives well; static sites do not handle interactive auth flows well. Using both — each for what it is good at — solves both problems cleanly. If only one is adopted initially, the static site is the better starting point: the version archive is the harder problem to retrofit.",
      "Pipeline integration",
      "The release pipeline (Phase 3, stage 7) publishes the spec after a successful production deploy. The publication is gated on deploy success — if production rolls back, documentation does not update. Documentation must reflect what is actually live.",
      "Base level recommendation: URI path versioning. Spec-first OpenAPI authoring with runtime conformance validation. openapi-diff in PR pipeline as the breaking-change gate. Three-phase lifecycle (GA, Maintenance, Retired) with default windows of three years major / one year minor support / patches on latest minor. Hosted documentation as a static site on Azure Storage with Front Door, organized by API version and release version. APIM developer portal added when an explicit external consumer program justifies its cost.",
    ],
    backQuestions: [
      "What is our official support window for major and minor API versions before they are retired?",
      "Is a three-year major support window realistic for a team this size, with no support function to backport fixes or manage consumer migrations?",
      "Who communicates deprecations and tracks consumer migration when no consumer-facing role exists?",
    ],
  },
  {
    id: 11,
    title: "The Cultural Commitment",
    part: "api",
    section: "2.6",
    frontContent: [
      "The mechanics in this section are individually straightforward. Azure provides good primitives. The team can implement URI versioning, breaking-change detection, and lifecycle phases in a quarter.",
      "What is harder is the commitment that goes with them. Once v1 is published, its contract is frozen. The team does not \"just fix\" v1 by changing it; consumers may have integrated against the published behavior. The team patches backward-compatibly within v1, or ships v2. This is a meaningful cultural shift from the implicit assumption — common in pre-product engineering organizations — that the team owns the API and can change it as needed.",
      "Engineering leadership air cover is what makes this commitment hold. The first time a developer says \"we just need to add this required field to v1 real quick,\" the answer needs to be no, and it needs to come from leadership, not from the engineer reviewing the PR. The breaking-change CI gate prevents the change from merging; leadership prevents the gate from being overridden under pressure. Both pieces are necessary.",
    ],
    backQuestions: [
      "How will leadership ensure the breaking-change CI gate is not overridden under pressure?",
      "Are we prepared to say \"no\" to expedient fixes that break published v1 contracts?",
      "Who specifically is \"engineering leadership\" for this purpose today — which named person provides the air cover this commitment depends on?",
    ],
  },
];
